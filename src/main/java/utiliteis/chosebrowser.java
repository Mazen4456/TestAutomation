/*
package utiliteis;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class chosebrowser {
    static WebDriver driver;

    private chosebrowser bowserOpnning (){
        if (driver.manage().window().maximize();
    }
}
*/
